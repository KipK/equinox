{
  "name": "equinox-card",
  "version": "0.1.0",
  "description": "Custom Lovelace card for Versatile Thermostat and standard climate entities.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "vite build && npm run build:history-lib && npm run gzip:dist",
    "build:history-lib": "node scripts/build-ha-better-history.mjs",
    "gzip:dist": "node scripts/gzip-dist.mjs"
  },
  "dependencies": {
    "@kipk/ha-better-history": "^0.1.0",
    "@kipk/load-ha-components": "^1.0.3",
    "lit": "^3.3.2"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^8.0.10"
  }
}
